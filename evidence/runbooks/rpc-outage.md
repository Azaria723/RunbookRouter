# RPC availability incident

Use when JSON-RPC requests fail, time out, or return sustained server errors across multiple methods.

Initial action: shift-rpc-traffic

Check upstream health, regional error rates and provider quotas before moving traffic to the standby endpoint.
